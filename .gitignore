node_modules/
dist/
build/
*.tsbuildinfo

# Environment & secrets (keep example only)
.env*
!.env.example

# Local Python venv (PaddleOCR)
.venv-paddle/
.playwright-browsers/

# Logs & OS
*.log
.DS_Store

# Credentials / keys
*credentials*
*.pem
*.p12
*.key
service-account*.json

# Generated case JSON files (auto-saved after each review)
case-library/cases/**/*.json
case-library/index/manifest.json
case-library/index/by-dimension/**

# Uploaded finding evidence (Phase 1 manual flow)
data/finding-evidence/files/**
data/finding-evidence/evidence/**
data/finding-evidence/links/**
data/finding-evidence/*-manifest.json
!data/finding-evidence/.gitkeep

# Keep structure and examples
!case-library/cases/.gitkeep
!case-library/index/.gitkeep
!case-library/examples/**
